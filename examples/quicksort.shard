let 
    quicksort = l: (case l of
        []         => [];
        [x, xs...] => let
                low = builtins.filter (a: a < x) xs;
                high = builtins.filter (a: a >= x) xs;
            in quicksort low ++ [x] ++ quicksort high
    );
in quicksort [ 4 2 6 8 9 1 3 0 5 7 ];

